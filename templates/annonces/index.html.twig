{% extends 'base.html.twig' %}

{% block title %}Annonces{% endblock %}

{% block body %}

    <section class="section-annonces">
        <div class="intro-annonces text-center my-4 g-4">
            <h1>Nos véhicules d'occasion</h1>
            <p>Pour une recherche personnalisée de votre véhicule, aidez vous des filtres ci-dessous.</p>
        </div>
        <div class="wrapper-filtres-annonces d-flex flex-column flex-xl-row">
            <div class="filtres">
                <p>ici se trouvera les filtres</p>
            </div>
            <div class="annonces col-12 col-lg-10 row row-cols-1 row-cols-lg-2 row-cols-xl-2 row-cols-xxl-3 mx-auto g-3 g-lg-4">
                {% for annonce in annonces %}
                    <div class="col">
                        <div class="card annonce-card">
                            <div class="card-body d-flex flex-column justify-content-around">
                            {% set imageAnnonce = annonce.pictures.first.name %}
                                <h3 class="card-title card-annonce-title">
                                {{ annonce.brands.name }}
								{{ annonce.models.name }}
								{{ annonce.title }}</h3>
                                <img src="images/product/{{imageAnnonce}}" alt="">
                            </div>
                            <div class="footer-card-annonce d-flex justify-content-between align-items-center">
                                <span>{{ annonce.price }} €</span>
                                <a href="{{ path('annonces_show', {'id': annonce.id}) }}">Détails</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>

    </section>
{% endblock %}
